{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<h1>Data_Dog Project</h1>
    {% if isLoged  %}
        <a href="{{ path('app_logout') }}">Logout</a>
    {% else %}
        <a href="{{ path('app_login') }}">Log in page</a>
    {% endif %}
<a href="{{ path('event_index') }}">Event CRUD</a>
    <a href="{{ path('category_index') }}">All categories</a>

    {% if isAdmin  %}
        <a href="{{ path('user_index') }}">All users</a>
    {% endif %}

{% endblock %}
