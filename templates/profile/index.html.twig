{% extends 'base.html.twig' %}


{% block title %}Profile{% endblock %}
{% block body %}
    <div class="container">

        <div  style="display: flex; align-content: center;">
            <h3 style="margin-top: 20px ">Welcome to your profile, {{ app.user.username }}!</h3>
        </div>
            <a class="btn btn-labeled btn-info" href="{{ path('app_passwordchange') }}">
                <span class="btn-label"></span>Change password
            </a>
    </div>
{% endblock %}

